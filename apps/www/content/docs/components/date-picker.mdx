---
title: Date Picker
description:
  Used to select dates or date ranges from a calendar, with support for
  formatting, localization, and inline calendar modes.
links:
  source: components/date-picker
  storybook: components-date-picker-picker--basic
  recipe: date-picker
---

<ExampleTabs name="date-picker-basic" />

## Features

- Single date, multiple dates, or date range selection
- Day, month, and year views for intuitive navigation
- Localization and locale-specific formatting
- Min/max constraints and unavailable dates
- Inline calendar mode for embedded experiences
- Fully keyboard navigable and accessible

:::note

Date values are powered by
[`@internationalized/date`](https://react-aria.adobe.com/internationalized/date/)
for timezone-safe, locale-aware date handling.

:::

## Usage

```tsx
import { DatePicker } from "@chakra-ui/react"
```

```tsx
<DatePicker.Root>
  <DatePicker.Label />
  <DatePicker.Control>
    <DatePicker.Input />
    <DatePicker.IndicatorGroup>
      <DatePicker.Trigger>
        <LuCalendar />
      </DatePicker.Trigger>
    </DatePicker.IndicatorGroup>
  </DatePicker.Control>
  <Portal>
    <DatePicker.Positioner>
      <DatePicker.Content>
        <DatePicker.View view="day">
          <DatePicker.Header />
          <DatePicker.DayTable />
        </DatePicker.View>
        <DatePicker.View view="month">
          <DatePicker.Header />
          <DatePicker.MonthTable />
        </DatePicker.View>
        <DatePicker.View view="year">
          <DatePicker.Header />
          <DatePicker.YearTable />
        </DatePicker.View>
      </DatePicker.Content>
    </DatePicker.Positioner>
  </Portal>
</DatePicker.Root>
```

## Picker

The popup mode renders the calendar in a dropdown triggered by an input or
button.

### Button Trigger

Replace the default trigger with a styled custom button.

<ExampleTabs name="date-picker-with-button" />

### Outside Icon

Place the trigger icon outside the input field.

<ExampleTabs name="date-picker-with-outside-icon" />

### Input Group

Integrate with InputGroup for custom input layouts with start/end elements.

<ExampleTabs name="date-picker-with-input-group" />

### Clear Icon

Add a clear trigger to reset the selection.

<ExampleTabs name="date-picker-with-clear" />

### Controlled

Control the selected date programmatically with `value` and `onValueChange`.

<ExampleTabs name="date-picker-controlled" />

### Default Value

Pre-populate the picker with an initial date in ISO 8601 format (`YYYY-MM-DD`).

<ExampleTabs name="date-picker-default-value" />

### Default View

Set the initial view when the calendar opens: `"day"`, `"month"`, or `"year"`.

<ExampleTabs name="date-picker-default-view" />

### Range Selection

Allow users to select a start and end date.

<ExampleTabs name="date-picker-range-selection" />

### Multiple Selection

Select multiple individual dates.

<ExampleTabs name="date-picker-multi-selection" />

### Month Range

Select a month rather than a single day.

<ExampleTabs name="date-picker-month-range" />

### Year Picker

Select a year from a range.

<ExampleTabs name="date-picker-year-picker" />

### Year Range

Select a year range.

<ExampleTabs name="date-picker-year-picker-range" />

### Min/Max

Restrict date selection to a specific range.

<ExampleTabs name="date-picker-min-max" />

### Unavailable Dates

Disable specific dates using a custom function (e.g., weekends, holidays).

<ExampleTabs name="date-picker-unavailable" />

### Formatting & Parsing

Control how dates are displayed and interpreted.

<ExampleTabs name="date-picker-format-parse" />

### Localization

Display the calendar in different languages with locale-specific formatting.

<ExampleTabs name="date-picker-locale" />

### Sizes

Pass the `size` prop to change the size of the date picker.

<ExampleTabs name="date-picker-with-sizes" />

### Variants

Pass the `variant` prop to change the visual style.

<ExampleTabs name="date-picker-with-variants" />

### Header Layout

Customize the header layout with range text and navigation buttons.

<ExampleTabs name="date-picker-with-header-layout" />

### Month and Year Select

Use dropdown selects for quick month/year navigation, useful for date of birth
selection.

<ExampleTabs name="date-picker-with-month-year-select" />

### Multiple Months

Show 2 or more months side by side for easier range selection.

<ExampleTabs name="date-picker-multiple-months" />

### Presets

Provide preset date options for quick selection.

<ExampleTabs name="date-picker-presets" />

### Presets Sidebar

A task-management style picker with presets sidebar and calendar side by side.

<ExampleTabs name="date-picker-with-presets-sidebar" />

### Today Button

Add a footer button to quickly jump to today's date.

<ExampleTabs name="date-picker-with-today-button" />

### With Time

Combine date selection with a time input for datetime picking.

<ExampleTabs name="date-picker-with-time" />

### Form

Integrate with native form validation.

<ExampleTabs name="date-picker-form" />

### React Hook Form

Integrate with React Hook Form using the Controller pattern.

<ExampleTabs name="date-picker-with-hook-form" />

### Provider

Set global configuration for all DatePickers within a subtree.

<ExampleTabs name="date-picker-root-provider" />

## Calendar

The inline mode renders the calendar directly on the page without a popup. Add
the `inline` prop to `DatePicker.Root`.

<ExampleTabs name="date-picker-inline" />

### Controlled

Control the inline calendar with `value` and `onValueChange`.

<ExampleTabs name="date-picker-inline-controlled" />

### Default Value

Pre-select a date when the calendar first renders.

<ExampleTabs name="date-picker-inline-default-value" />

### Range Selection

Select a start and end date in the inline calendar.

<ExampleTabs name="date-picker-inline-range-selection" />

### Multi Selection

Select multiple individual dates.

<ExampleTabs name="date-picker-inline-multi-selection" />

### Min and Max

Restrict the selectable date range.

<ExampleTabs name="date-picker-inline-min-max" />

### Unavailable Dates

Disable specific dates based on custom logic.

<ExampleTabs name="date-picker-inline-unavailable" />

### Multiple Months

Display two or more months side by side.

<ExampleTabs name="date-picker-inline-multiple-months" />

### Locale

Customize for different languages and regional formats.

<ExampleTabs name="date-picker-inline-locale" />

### Booking Time Grid

A Calendly-style booking interface with an inline calendar and a scrollable time
slot grid. Pick a date to reveal available times, then click a slot to select.

<ExampleTabs name="date-picker-with-time-grid" />

## Props

### Root

<PropTable component="DatePicker" part="Root" />

### Input

<PropTable component="DatePicker" part="Input" />

### View

<PropTable component="DatePicker" part="View" />

### TableCell

<PropTable component="DatePicker" part="TableCell" />

### PresetTrigger

<PropTable component="DatePicker" part="PresetTrigger" />

## Explorer

Explore the `DatePicker` component parts interactively. Click on parts in the
sidebar to highlight them in the preview.

<Explorer name="date-picker-explorer-demo" />
