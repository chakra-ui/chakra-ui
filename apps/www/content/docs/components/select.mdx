---
title: Select
description:
  Select component is a component that allows users pick a value from predefined
  options.
links:
  [
    { title: "Source", url: "#" },
    { title: "Storybook", url: "#" },
    { title: "Recipe", url: "#" },
  ]
---

<ExampleTabs name="select-basic" />

## Setup

If you don't already have the snippet, run the following command to add the
`select` snippet

```sh
chakra snippet add select
```

The snippet includes a closed component composition for the `Select` component.

## Usage

```jsx
import {
  SelectContent,
  SelectItem,
  SelectLabel,
  SelectRoot,
  SelectTrigger,
  SelectValueText,
} from "@/components/ui/select"
```

```jsx
<SelectRoot items={[]}>
  <SelectLabel />
  <SelectTrigger>
    <SelectValueText placeholder="Select" />
  </SelectTrigger>
  <SelectContent>
    <SelectItem />
  </SelectContent>
</SelectRoot>
```

## Examples

### Select Within Popover

Use the `Select` within a `Popover` component.

<ExampleTabs name="select-in-popover" />

### Select Within Avatar

You can compose the `Select` and the `Avatar`.

<ExampleTabs name="select-with-avatar" />

### Select With Clear

Pass the `clearable` prop to the a `SelectTrigger`

<ExampleTabs name="select-with-clear" />

### Select With Overflow

<ExampleTabs name="select-with-overflow" />
