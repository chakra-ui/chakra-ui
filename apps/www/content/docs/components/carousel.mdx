---
title: Carousel
description: Used to display multiple items or images in a sliding view.
links:
  source: components/carousel
  storybook: components-carousel--basic
  recipe: carousel
  ark: https://ark-ui.com/docs/components/carousel
---

<ExampleTabs name="carousel-basic" />

## Usage

```jsx
import { Carousel } from "@chakra-ui/react"
```

```jsx
<Carousel.Root>
  <Carousel.ItemGroup>
    <Carousel.Item />
  </Carousel.ItemGroup>

  <Carousel.Control>
    <Carousel.PrevTrigger />
    <Carousel.NextTrigger />
  </Carousel.Control>

  <Carousel.IndicatorGroup>
    <Carousel.Indicator />
  </Carousel.IndicatorGroup>
</Carousel.Root>
```

## Examples

### With Indicators

Use indicators to show which slide is currently active.

<ExampleTabs name="carousel-with-indicators" />

### Spacing

Add consistent spacing between slides using the `spacing` prop. You can adjust
the spacing interactively with a slider in this example.

<ExampleTabs name="carousel-spacing" />

### Slides per page

Display multiple slides in view at once using the slidesPerPage prop. You can
also control how many slides move per navigation action using slidesPerMove.

<ExampleTabs name="carousel-slides-per-page" />

### Controlled

Control the carousel's active page externally using the `page` and
`onPageChange` props for custom state management.

<ExampleTabs name="carousel-controlled" />
