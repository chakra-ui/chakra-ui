---
title: Tags Input
description:
  Used to enter multiple values as tags with features like tag creation,
  deletion, and keyboard navigation.
links:
  source: components/tags-input
  storybook: components-tags-input--basic
  recipe: tags-input
  ark: https://ark-ui.com/docs/components/tags-input
---

<ExampleTabs name="tags-input-basic" />

## Usage

```jsx
import { TagsInput } from "@chakra-ui/react"
```

```jsx
<TagsInput.Root>
  <TagsInput.Label />
  <TagsInput.Control>
    <TagsInput.Context>
      {({ value }) =>
        value.map((tag, index) => (
          <TagsInput.Item key={index} index={index} value={tag}>
            <TagsInput.ItemPreview>
              <TagsInput.ItemText>{tag}</TagsInput.ItemText>
              <TagsInput.ItemDeleteTrigger />
            </TagsInput.ItemPreview>
            <TagsInput.ItemInput />
          </TagsInput.Item>
        ))
      }
    </TagsInput.Context>
    <TagsInput.Input />
    <TagsInput.ClearTrigger />
  </TagsInput.Control>
  <TagsInput.HiddenInput />
</TagsInput.Root>
```

## Examples

### Controlled

Control the tags input value externally using the `value` and `onValueChange`
props for custom state management.

<ExampleTabs name="tags-input-controlled" />

### With Store

An alternative way to control the tags input is to use the `RootProvider`
component and the `useTagsInput` store hook.

This way you can access the tags input state and methods from outside the
component.

> Use `RootProvider + useTagsInput` or `Root`, not both.

<ExampleTabs name="tags-input-with-store" />

### Max Tags

Limit the number of tags that can be added using the `max` prop to prevent
excessive input and maintain a clean interface.

<ExampleTabs name="tags-input-max-tags" />

### Validation

Implement custom validation rules to ensure tags meet specific criteria before
being added to the input.

<ExampleTabs name="tags-input-validation" />

### Disabled

Disable the tags input to prevent user interaction while maintaining visual
context of existing tags.

<ExampleTabs name="tags-input-disabled" />

### Read Only

Make the tags input read-only to display tags without allowing editing, useful
for viewing assigned tags or labels.

<ExampleTabs name="tags-input-read-only" />

### With Paste

Allow users to paste multiple values at once, automatically splitting them into
individual tags based on a delimiter.

<ExampleTabs name="tags-input-with-paste" />

### With Blur Behavior

Configure how the input behaves when it loses focus, such as automatically
creating a tag from the current input value.

<ExampleTabs name="tags-input-with-blur" />

### With Custom Delimiter

Use custom delimiters like commas, semicolons, or spaces to create new tags as
users type.

<ExampleTabs name="tags-input-with-delimiter" />

### Editable Tags

Enable inline editing of existing tags by clicking on them, allowing users to
quickly update tag values.

<ExampleTabs name="tags-input-editable" />

### With Colors

Add visual distinction to tags using different colors, perfect for categorizing
or prioritizing tags.

<ExampleTabs name="tags-input-with-colors" />

## Props

## Explorer

Explore the `TagsInput` component parts interactively. Click on parts in the
sidebar to highlight them in the preview.

<Explorer name="tags-input-explorer" />
