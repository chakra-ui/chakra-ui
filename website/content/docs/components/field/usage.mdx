---
id: field
category: form
title: Field
package: '@chakra-ui/react'
description:
  Field provides context such as `invalid`, `disabled`, and `required` to form
  elements
---

## Import

```js
import { Field } from '@chakra-ui/react'
```

## Usage

```jsx
<Field>
  <Label>Email address</Label>
  <Input type='email' />
  <HelpText>We'll never share your email.</HelpText>
</Field>
```

### Usage with `RadioGroup`

```jsx
<Field as='fieldset'>
  <Label as='legend'>Favorite Naruto Character</Label>
  <RadioGroup.Root defaultValue='Itachi'>
    <HStack gap='24px'>
      <RadioGroup.Item value='Sasuke'>Sasuke</RadioGroup.Item>
      <RadioGroup.Item value='Nagato'>Nagato</RadioGroup.Item>
      <RadioGroup.Item value='Itachi'>Itachi</RadioGroup.Item>
      <RadioGroup.Item value='Sage of the six Paths'>
        Sage of the six Paths
      </RadioGroup.Item>
    </HStack>
  </RadioGroup.Root>
  <HelpText>Select only if you're a fan.</HelpText>
</Field>
```

### Displaying an error message

`Field.ErrorMessage` will only show up when the property `invalid` in `Field` is
`true`.

```jsx
function ErrorMessageExample() {
  const [input, setInput] = useState('')

  const handleInputChange = (e) => setInput(e.target.value)
  const invalid = input === ''

  return (
    <Field invalid={invalid}>
      <Label>Email</Label>
      <Input type='email' value={input} onChange={handleInputChange} />
      {!isError ? (
        <HelpText>
          Enter the email you'd like to receive the newsletter on.
        </HelpText>
      ) : (
        <ErrorMessage>Email is required.</ErrorMessage>
      )}
    </Field>
  )
}
```

### Making a field required

By passing the `required` props, the `Input` field has `aria-required` set to
`true`, and the `Field.Label` will show a red asterisk.

This red asterisk can be overwritten by passing `requiredIndicator` to the
`Field.Label`. If you want to indicate that a field is optional you can add
`optionalIndicator` to the `Field.Label`

```jsx
<Field required>
  <Label>First name </Label>
  <Input placeholder='First name' />
</Field>
```

### Usage with `Select`

```jsx
<Field>
  <Label>Country</Label>
  <NativeSelect.Root>
    <NativeSelect.Field placeholder='Select country'>
      <option>United Arab Emirates</option>
      <option>Nigeria</option>
    </NativeSelect.Field>
  </NativeSelect.Root>
</Field>
```

### Usage with `NumberInput`

```jsx
<Field>
  <Label>Amount</Label>
  <NumberInput.Root max={50} min={10}>
    <NumberInput.Field />
    <NumberInput.Control>
      <NumberInput.IncrementTrigger />
      <NumberInput.DecrementTrigger />
    </NumberInput.Control>
  </NumberInput.Root>
</Field>
```
