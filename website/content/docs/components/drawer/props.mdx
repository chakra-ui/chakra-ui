---
id: drawer
scope: props
---

## Props

### Drawer Props

`Drawer` composes the `Dialog` component with these extra props:

<PropsTable of='DrawerRoot' />

### Other components

- `Drawer.Overlay`, `Drawer.Footer`, `Drawer.Header` and `Drawer.Body` composes
  `Box` component
- `Drawer.CloseButton` composes `CloseButton`
