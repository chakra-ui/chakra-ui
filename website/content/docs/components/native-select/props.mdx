---
id: native-select
scope: props
---

## Props

The `NativeSelect.Root` component composes [Box](/docs/components/layout/box) so
you can pass all `Box` props, and native `Select` props in addition to these:

<PropsTable of='NativeSelectRoot' />
