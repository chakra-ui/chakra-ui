---
id: popover
scope: props
---

## Props

### Popover Props

<PropsTable of='PopoverRoot' omit={['enabled']} />

### Other Props

- `Popover.Content` composes `Box`.
- `Popover.Positioner` has the ability to smartly position itself. Thanks to
  popper.js.
- `Popover.Arrow`, `Popover.Header`, `Popover.Footer` and `Popover.Body`
  composes `Box`.
- `Popover.CloseTrigger` composes `Box` component.
