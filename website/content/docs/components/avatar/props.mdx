---
id: avatar
scope: props
---

## Props

### Avatar.Root Props

`Avatar.Root` composes the `Box` component so you can pass all its props. These
are props specific to the `Avatar.Root` component:

<PropsTable of='Avatar' />

### Avatar.Group Props

`Avatar.Group` composes the `Box` component so you can pass all its props. These
are props specific to the `Avatar.Group` component:

<PropsTable of='AvatarGroup' />
