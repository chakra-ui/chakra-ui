---
id: menu
scope: props
---

## Props

### Menu.Root Props

<PropsTable of='MenuRoot' />

### Menu.Trigger Props

`Menu.Trigger` composes [Box](/docs/components/layout/box) so you can pass all
`Box` props to change its style.

### Menu.List Props

`Menu.List` composes [Box](/docs/components/layout/box) so you can pass all
`Box` props to change its style.

<PropsTable of='MenuList' />

### Menu.Item Props

<PropsTable of='MenuItem' />

### Menu.Group Props

`Menu.Group` composes [Box](/docs/components/layout/box) so you can pass all
`Box` props to change its style.

### Menu.OptionGroup Props

<PropsTable of='MenuOptionGroup' />

### Menu.ItemOption Props

`Menu.ItemOption` composes [Box](/docs/components/layout/box) so you can pass
all box props in addition to these:

<PropsTable of='Menu.ItemOption' />
