import badge from "./badge"
const { variants, defaultProps } = badge

const baseStyle = {
  fontFamily: "mono",
  fontSize: "sm",
  px: "0.2em",
  borderRadius: "sm",
}

const code = {
  baseStyle,
  variants,
  defaultProps,
}

export default code
