const parts = ["container", "item", "icon"]

const baseStyleContainer = {}

const baseStyleItem = {}

const baseStyleIcon = {}

const baseStyle = {
  container: baseStyleContainer,
  item: baseStyleItem,
  icon: baseStyleIcon,
}

export default {
  parts,
  baseStyle,
}
