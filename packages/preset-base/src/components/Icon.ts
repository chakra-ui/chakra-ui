import { ComponentTheme } from "./utils"

const Icon: ComponentTheme = {
  defaultProps: {
    size: "md",
  },
  sizes: {
    xs: { boxSize: "16px" },
    sm: { boxSize: "24px" },
    md: { boxSize: "32px" },
    lg: { boxSize: "40px" },
    xl: { boxSize: "48px" },
  },
}

export default Icon
