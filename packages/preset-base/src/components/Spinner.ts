import { ComponentTheme } from "./utils"

const Spinner: ComponentTheme = {
  variantSize: {
    xs: { size: "0.75rem" },
    sm: { size: "1rem" },
    md: { size: "1.5rem" },
    lg: { size: "2rem" },
    xl: { size: "3rem" },
  },
}

export default Spinner
