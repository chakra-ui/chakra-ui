# NumberInput

The NumberInput component is similar to the [Input](/input), but it contains
controls for incrementing or decrementing numeric values.

## Import

```js
import { NumberInput } from "@chakra-ui/core";
```

## Usage

```jsx
<NumberInput />
```

### With minimum value

```jsx
<NumberInput defaultValue={15} min={10} />
```

### Sizes

```jsx
<Stack>
  <NumberInput size="sm" defaultValue={15} min={10} />
  <NumberInput size="md" defaultValue={15} min={10} />
  <NumberInput size="lg" defaultValue={15} min={10} />
</Stack>
```

### With maximum value

```jsx
<NumberInput defaultValue={20} max={25} />
```

### With step value

```jsx
<NumberInput defaultValue={20} step={5} max={50} />
```

### Combining it with a Slider

```jsx
function SliderInput() {
  const [value, setValue] = React.useState(0);
  const handleChange = value => setValue(value);

  return (
    <Flex>
      <NumberInput
        maxW="100px"
        mr="2rem"
        value={value}
        onChange={handleChange}
      />
      <Slider flex="1" value={value} onChange={handleChange}>
        <SliderTrack />
        <SliderFilledTrack />
        <SliderThumb
          fontSize="sm"
          width="32px"
          height="20px"
          children={value}
        />
      </Slider>
    </Flex>
  );
}
```

## Props

NumberInput composes `Input` with some additional props listed below.

| Name           | Type                      | Default | Description                                                                                                                     |
| -------------- | ------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------- |
| `min`          | `number`                  |         | Standard `input` min attribute, to be used with `type="number"`                                                                 |
| `max`          | `number`                  |         | Standard `input` max attribute, to be used with `type="number"`                                                                 |
| `step`         | `number`                  |         | Standard `input` step attribute, to be used with `type="number"`                                                                |
| `placeholder`  | `string`                  |         | Text to display in the input if the `input` is empty.                                                                           |
| `defaultValue` | `string`, `number`        |         | Initial value of the input, for uncontrolled usage.                                                                             |
| `value`        | `string`, `number`        |         | The value of the input, for controlled usage.                                                                                   |
| `isDisabled`   | `boolean`                 | `false` | If `true`, the input will be disabled. This sets `aria-disabled=true` and you can style this state by passing `_disabled` prop. |
| `onChange`     | `(value: number) => void` |         | Function called when the value of the `input` changes, for controlled usage.                                                    |
