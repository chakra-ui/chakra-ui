;(function () {
  try {
    var a = function (o) {
        var v = "(prefers-color-scheme: dark)",
          h = window.matchMedia(v).matches ? "dark" : "light",
          e = o === "system" ? h : o,
          s = document.documentElement,
          l = document.body,
          d = "chakra-ui-light",
          m = "chakra-ui-dark",
          i = e === "dark"
        l.classList.add(i ? m : d),
          l.classList.remove(i ? d : m),
          (s.style.colorScheme = e),
          (s.dataset.theme = e)
      },
      n = a,
      r = "${init}",
      t = "${key}",
      c = localStorage.getItem(t)
    c ? a(c) : (a(r), localStorage.setItem(t, r))
  } catch (a) {}
})()
