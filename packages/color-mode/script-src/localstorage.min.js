;(function () {
  try {
    var a = function (c) {
        var v = "(prefers-color-scheme: dark)",
          h = window.matchMedia(v).matches ? "dark" : "light",
          e = c === "system" ? h : c,
          o = document.documentElement,
          s = document.body,
          l = "chakra-ui-light",
          d = "chakra-ui-dark",
          i = e === "dark"
        s.classList.add(i ? d : l),
          s.classList.remove(i ? l : d),
          (o.style.colorScheme = e),
          (o.dataset.theme = e)
      },
      n = a,
      m = "${init}",
      r = "${key}",
      t = localStorage.getItem(r)
    t ? a(t) : localStorage.setItem(r, a(m))
  } catch (a) {}
})()
