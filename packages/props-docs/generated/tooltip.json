{
  "TooltipRoot": {
    "arrowPadding": {
      "defaultValue": "8",
      "required": false,
      "description": "The padding required to prevent the arrow from\nreaching the very edge of the popper."
    },
    "arrowShadowColor": { "required": false },
    "arrowSize": { "defaultValue": "10", "required": false },
    "closeDelay": {
      "defaultValue": "0ms",
      "required": false,
      "description": "Delay (in ms) before hiding the tooltip"
    },
    "closeOnClick": {
      "defaultValue": true,
      "required": false,
      "description": "If `true`, the tooltip will hide on click"
    },
    "closeOnEsc": {
      "defaultValue": true,
      "required": false,
      "description": "If `true`, the tooltip will hide on pressing Esc key"
    },
    "closeOnPointerDown": {
      "defaultValue": true,
      "required": false,
      "description": "If `true`, the tooltip will hide while the pointer is down"
    },
    "closeOnScroll": { "defaultValue": false, "required": false },
    "defaultOpen": {
      "defaultValue": false,
      "required": false,
      "description": "If `true`, the tooltip will be initially shown"
    },
    "disabled": { "defaultValue": false, "required": false },
    "gutter": {
      "defaultValue": "8",
      "required": false,
      "description": "The distance or margin between the reference and popper.\nIt is used internally to create an `offset` modifier.\n\nNB: If you define `offset` prop, it'll override the gutter."
    },
    "id": {
      "required": false,
      "description": "Custom `id` to use in place of `uuid`"
    },
    "modifiers": {
      "required": false,
      "description": "Array of popper.js modifiers. Check the docs to see\nthe list of possible modifiers you can pass."
    },
    "offset": {
      "required": false,
      "description": "The main and cross-axis offset to displace popper element\nfrom its reference element."
    },
    "onClose": {
      "required": false,
      "description": "Callback to run when the tooltip hides"
    },
    "onOpen": {
      "required": false,
      "description": "Callback to run when the tooltip shows"
    },
    "open": {
      "defaultValue": false,
      "required": false,
      "description": "If `true`, the tooltip will be shown (in controlled mode)"
    },
    "openDelay": {
      "defaultValue": "0ms",
      "required": false,
      "description": "Delay (in ms) before showing the tooltip"
    },
    "placement": {
      "defaultValue": "bottom",
      "required": false,
      "description": "The placement of the popper relative to its reference."
    }
  },
  "TooltipTrigger": {},
  "TooltipContent": {
    "motionProps": {
      "type": "HTMLMotionProps<\"div\">",
      "required": false,
      "description": "The motion props for the tooltip"
    }
  },
  "TooltipArrow": {},
  "TooltipPositioner": {
    "portalProps": {
      "type": "Pick<PortalProps, \"containerRef\">",
      "required": false,
      "description": "Props to be forwarded to the portal component"
    }
  },
  "UseTooltip": {
    "arrowPadding": {
      "defaultValue": "8",
      "required": false,
      "description": "The padding required to prevent the arrow from\nreaching the very edge of the popper."
    },
    "arrowShadowColor": { "type": "string", "required": false },
    "arrowSize": { "type": "number", "defaultValue": "10", "required": false },
    "closeDelay": {
      "type": "number",
      "defaultValue": "0ms",
      "required": false,
      "description": "Delay (in ms) before hiding the tooltip"
    },
    "closeOnClick": {
      "type": "boolean",
      "defaultValue": true,
      "required": false,
      "description": "If `true`, the tooltip will hide on click"
    },
    "closeOnEsc": {
      "type": "boolean",
      "defaultValue": true,
      "required": false,
      "description": "If `true`, the tooltip will hide on pressing Esc key"
    },
    "closeOnPointerDown": {
      "type": "boolean",
      "defaultValue": true,
      "required": false,
      "description": "If `true`, the tooltip will hide while the pointer is down"
    },
    "closeOnScroll": {
      "type": "boolean",
      "defaultValue": false,
      "required": false
    },
    "defaultOpen": {
      "type": "boolean",
      "defaultValue": false,
      "required": false,
      "description": "If `true`, the tooltip will be initially shown"
    },
    "disabled": { "type": "boolean", "defaultValue": false, "required": false },
    "gutter": {
      "defaultValue": "8",
      "required": false,
      "description": "The distance or margin between the reference and popper.\nIt is used internally to create an `offset` modifier.\n\nNB: If you define `offset` prop, it'll override the gutter."
    },
    "id": {
      "type": "string",
      "required": false,
      "description": "Custom `id` to use in place of `uuid`"
    },
    "modifiers": {
      "required": false,
      "description": "Array of popper.js modifiers. Check the docs to see\nthe list of possible modifiers you can pass."
    },
    "offset": {
      "required": false,
      "description": "The main and cross-axis offset to displace popper element\nfrom its reference element."
    },
    "onClose": {
      "type": "void",
      "required": false,
      "description": "Callback to run when the tooltip hides"
    },
    "onOpen": {
      "type": "void",
      "required": false,
      "description": "Callback to run when the tooltip shows"
    },
    "open": {
      "type": "boolean",
      "defaultValue": false,
      "required": false,
      "description": "If `true`, the tooltip will be shown (in controlled mode)"
    },
    "openDelay": {
      "type": "number",
      "defaultValue": "0ms",
      "required": false,
      "description": "Delay (in ms) before showing the tooltip"
    },
    "placement": {
      "defaultValue": "bottom",
      "required": false,
      "description": "The placement of the popper relative to its reference."
    }
  }
}
