Here's the hooks alternative we should work on

```jsx
const {
  accordion,
  accordionItem,
  accordionHeader,
  accordionPanel,
} = useAccordion();

const { isExpanded, onClose } = useAccordionItemState();
```
