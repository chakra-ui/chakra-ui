## 13-08-2022

`@chakra-ui/react@2.2.7`

- Remove `ChakraProvider.defaultProps` and move logic inside the component.

**Storybook Addon** `v4.0.7`

- Fix issue with nullish operator in storybook

**Theme Tools** `v2.0.8`

- Fix regression where `StyleFunctionProps` type was not exported

**Theme** `v2.1.6`

- Improve contrast of `FormHelperText` to satisfy accessibility check
- Fix issue where disabled input with flushed or unstyled variant isn't
  perceivable.

**Accordion** `v2.0.9`

- Reduce bundle size of accordion by leverage smaller packages

**Utils** `v2.0.7`

- Add pure annotation to utils package
- Add pure annotation to `isBrowser` to tree-shake when bundling

**React Use Update Effect** `v2.0.1`

- Initial release

**Color Mode** `v2.1.5`

- Fix regression where `localStorageManager` was not exported

**Merge Utils** `v2.0.1`

- Initial release

**Object Utils** `v2.0.1`

- Initial release

**React Env** `v2.0.7`

- Omit rendered env span when the environment prop is passed. - Add `id` and
  `hidden` attributes to the rendered env span

**Event Utils** `v2.0.1`

- Initial release

**Number Utils** `v2.0.1`

- Initial release

**React Types** `v2.0.1`

- Initial release

**React Use Event Listener** `v2.0.1`

- Initial release

**Styled System** `v2.2.6`

- Improve style computation performance by looping over styles once.

Previously, we had two steps in the logic (expand responsive and convert to css
object). This can be quite expensive with large css objects (or style props).

We now process both steps in a single pass.

- Fix regression where `ThemeTypings` created by chakra-cli could not be used
- Escape symbols in css variable to make it works

**Alert** `v2.0.7`

- Refactor to reduce bundle size

**Avatar** `v2.0.8`

- Refactor to reduce bundle size

**Hooks** `v2.0.7`

- Deprecate `useDimensions` in favor of the `useSize` hook

**React Use Size** `v2.0.1`

- Initial release

**React Use Disclosure** `v2.0.1`

- Initial release

**Cli** `v2.1.5`

- Fix regression where `ThemeTypings` created by chakra-cli could not be used

**React Use Callback Ref** `v2.0.1`

- Initial release

**React Use Controllable State** `v2.0.1`

- Initial release

**React Context** `v2.0.1`

- Initial release

**React Use Merge Refs** `v2.0.1`

- Initial release

**React Use Pan Event** `v2.0.1`

- Initial release
